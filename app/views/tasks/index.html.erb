<h1>Task list</h1>
<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to "#{task.title}", task_path(task.id) %></li>
  <% end %>
</ul>

<%= link_to "Add new Task", new_task_path, class: 'btn btn-secondary' %>
